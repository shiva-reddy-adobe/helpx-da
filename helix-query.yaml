auto-generated: true
indices:
  articles:
    include:
      - /**
    exclude:
      - /fragments/**
      - /drafts/**
      - /tools/**
    target: /articles/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
      product:
        select: head > meta[name="product"]
        value: |
          attribute(el, "content")
      template:
        select: head > meta[name="template"]
        value: |
          attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      content:
        select: main > div
        values: characters(textContent(el), 0, 200)
      tags:
        select: head > meta[property="article:tag"]
        values: |
          attribute(el, 'content')
  product-hubs:
    include:
      - /products/**
    target: /products/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      product:
        select: head > meta[name="product"]
        value: |
          attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
  tutorials:
    include:
      - /tutorials/**
    target: /tutorials/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      product:
        select: head > meta[name="product"]
        value: |
          attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
      tags:
        select: head > meta[property="article:tag"]
        values: |
          attribute(el, 'content')
  error-codes:
    include:
      - /error-codes/**
    target: /error-codes/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      product:
        select: head > meta[name="product"]
        value: |
          attribute(el, "content")
      errorCode:
        select: head > meta[name="error-code"]
        value: |
          attribute(el, "content")
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
  whats-new:
    include:
      - /whats-new/**
    target: /whats-new/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      product:
        select: head > meta[name="product"]
        value: |
          attribute(el, "content")
      releaseDate:
        select: head > meta[name="release-date"]
        value: |
          dateValue(attribute(el, 'content'), 'MM-DD-YYYY')
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
